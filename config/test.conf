server {
	listen 8080 ;
	server_name default_server;
	errorpage 301 https://42tokyo.jp;
	
	location / {
		method GET;
		autoindex on;
		index nginx.html;
	}

	location /html/ {
		method GET;
		root /html/;
		autoindex on;
		index index.html;
	}
}
